<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/admin.css">
    <link rel="stylesheet" href="/css/userProfileCard.css">
</head>
<body>
    <aside class="sidebar">
      <div class="logo">Users</div>
      <nav>
        <ul class="nav-menu">
          <li class="nav-item">
            <a href="http://localhost:8080/companies/<%= id %>/admins/<%= adminId %>" class="nav-link active">
              <svg class="nav-icon" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                  d="M4 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2V6zM14 6a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2V6zM4 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2H6a2 2 0 01-2-2v-2zM14 16a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 01-2-2v-2z" />
              </svg>
              Dashboard
            </a>
          </li>

        </ul>
      </nav>
    </aside>
    <% for(let user of users) { %>
    <div class="card userProfileCard">
            <div class="user-profile">
                <div class="user-avatar"><%= user.name[0] %></div>
                <div class="user-info">
                    <div class="user-name"><%= user.name %></div>
                    <div class="user-role">
                        <% if(user.role === "user") { %>
                            EMPLOYEE • <%= user.companyId.name %>
                        <% } else { %>
                            MANAGER • <%= user.companyId.name %>
                        <% } %>
                    </div>
                </div>
                <a class="btn btn-outline" href="http://localhost:8080/companies/<%= user.companyId._id %>/admins/<%= user.adminId %>/users/<%= user._id %>/expenses">View Expense Record</a>
            </div>
        </div>
    <% } %>
</body>
</html>